# pubmedqa_biobert
PubMedBERT fine-tuning on PubMedQA for biomedical question answering (yes/no/maybe classification). Two-phase training with layer-wise LR decay, class weighting, and label smoothing. Achieves 75.8% accuracy on PQA-L test set.
